name: Model ops template

# Can have a docker_env instead of a conda_env, e.g.
docker_env:
   image: modelops_template

entry_points:
  main:
    parameters:
      data_file: path
    command: "python worker.py {data_file}"